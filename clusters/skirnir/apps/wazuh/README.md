# wazuh

Wazuh is planned but not deployed yet in this repo.

## Current status

- No `compose.yaml` is defined for this stack yet.
- Proxy/Homepage references are intentionally disabled until the stack is added.

## TODO before enabling

- Add `apps/wazuh/compose.yaml`
- Re-enable Wazuh route in `apps/proxy/Caddyfile`
- Re-add Wazuh tile in `apps/homepage/config/services.yaml`
